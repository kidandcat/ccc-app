# CCC

Flutter app for [Crew Command Center](https://github.com/kidandcat/ccc). Pair a machine with `ccc pair`, pick it in the app, talk to the bots.

The public hub (`wss://hub.getccc.dev`) is only a pipe. Messages are NaCl boxes; the hub cannot read them. Telegram tokens never leave the machine.
